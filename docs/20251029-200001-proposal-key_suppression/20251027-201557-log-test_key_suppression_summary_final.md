# Итоговый саммари: Подавление клавиатурных событий в Linux

## Цель проекта
Реализовать приложение, которое:
1. Перехватывает комбинацию RCtrl+Backspace
2. Показывает уведомление
3. **НЕ отдаёт** эту комбинацию в другие приложения
4. Пропускает все остальные клавиши в систему
5. Сохраняет автоповтор клавиш

## Тестированные подходы

### 1. pynput с suppress=True + эмуляция через Controller
**Что делали:**
- `suppress=True` для перехвата всех событий
- Эмуляция остальных событий обратно через `Controller`
- Фильтрация injected событий по флагу

**Проблемы:**
- ❌ События переставали доходить в другие приложения после первой сработавшей комбинации
- ❌ Бесконечная рекурсия при обработке injected событий
- ❌ `return False` останавливал listener, а `return True` не подавлял события

**Вывод:** pynput не предназначен для точечного подавления, только для мониторинга

### 2. pynput с suppress=False + return False
**Что делали:**
- `suppress=False` для получения событий без блокировки
- `return False` для подавления конкретных событий

**Проблемы:**
- ❌ `return False` не подавлял событие, оно всё равно доходило до других приложений
- ❌ `return False` останавливал listener (завершало программу)

**Вывод:** Нет способа точечно подавить событие, не остановив listener

### 3. evdev + uinput (ФИНАЛЬНОЕ РЕШЕНИЕ)
**Что делали:**
- `device.grab()` для захвата физической клавиатуры
- Чтение событий в цикле `device.read_loop()`
- Создание виртуального устройства через `uinput` для эмуляции
- Фильтрация RCtrl+Backspace
- Эмуляция остальных событий обратно в систему

**Преимущества:**
- ✅ Точечное подавление конкретной комбинации
- ✅ Все остальные клавиши работают нормально
- ✅ Работает на Wayland и X11
- ✅ Нет проблем с injected событиями (читаем только из одного устройства)

## Текущая реализация

### Архитектура
```
Физическая клавиатура (event6)
    ↓ [device.grab()] - захватываем, получаем ВСЕ события
Наше приложение
    ↓ [фильтрация RCtrl+Backspace] - подавляем
    ↓ [ui.write + ui.syn] - эмулируем остальные
Виртуальное uinput устройство
    ↓
Система и другие приложения
```

### Логика обработки событий

#### Состояния клавиш в evdev:
- `key_down` (value=1) - нажатие
- `key_hold` (value=2) - автоповтор
- `key_up` (value=0) - отпускание

#### Наша стратегия эмуляции:

**Press (key_down):**
```python
ui.write(e.EV_KEY, event.code, event.value)  # Записываем, НЕ синхронизируем
```
- ✅ Накопление события в буфере
- ✅ НЕ вызываем `ui.syn()` — это критично!

**Repeat (key_hold):**
```python
ui.write(e.EV_KEY, event.code, 2)  # Значение 2 для повтора
ui.syn()  # Синхронизируем
```
- ✅ Отправляем каждое повторение с синхронизацией
- ✅ Автоповтор работает корректно

**Release (key_up):**
```python
ui.write(e.EV_KEY, event.code, event.value)  # Записываем
ui.syn()  # Синхронизируем полную пару press+release
```
- ✅ Завершаем полную пару событий
- ✅ Отправляем press+release вместе в одном пакете

### Проблема пустых строк

**Что было:**
Вызывали `ui.syn()` после КАЖДОГО события:
```python
# ❌ НЕПРАВИЛЬНО
ui.write(e.EV_KEY, event.code, 1)  # Press
ui.syn()  # Отправляем пустой пакет [Press]
ui.write(e.EV_KEY, event.code, 0)  # Release  
ui.syn()  # Отправляем пустой пакет [Release]
```

**Проблема:**
- `ui.syn()` отправляет синхронизационное событие
- Каждый вызов `syn()` создаёт отдельный пакет событий
- Микропакеты `[Press]` → `[Release]` вместо полной пары `[Press, Release]`
- Система интерпретировала это как "пустой ввод"
- В редакторе Cursor появлялись пустые строки

**Что стало:**
Вызываем `ui.syn()` только после полных пакетов:
```python
# ✅ ПРАВИЛЬНО
ui.write(e.EV_KEY, event.code, 1)  # Press (буфер)
# ... время проходит ...
ui.write(e.EV_KEY, event.code, 0)  # Release (в том же буфере)
ui.syn()  # Отправляем полную пару [Press, Release] одним пакетом
```

**Результат:**
- ✅ События отправляются полными пакетами
- ✅ Нет пустых строк в редакторе
- ✅ Автоповтор работает (repeat события с syn())
- ✅ Клавиши работают нормально

### Комбинация RCtrl+Backspace

**Логика подавления:**
1. Добавляем все клавиши в `pressed_keys` при нажатии
2. Проверяем: есть ли RCtrl (e.KEY_RIGHTCTRL) и Backspace (e.KEY_BACKSPACE) одновременно
3. Если да:
   - Показываем уведомление
   - Удаляем Backspace из `pressed_keys`
   - Делаем `continue` — НЕ эмулируем
4. В других случаях эмулируем через uinput

**Результат:**
- ✅ RCtrl+Backspace перехватывается, не доходит до других приложений
- ✅ Все остальные клавиши работают нормально
- ✅ Автоповтор работает
- ✅ Нет пустых строк

## Выводы

### Почему pynput не работает
- Нет механизма выборочного подавления
- `suppress=True` блокирует всё, требует эмуляции всего обратно
- `return False` останавливает listener, не подавляет событие
- Проблемы с injected событиями и рекурсией

### Почему evdev+uinput работает
- Низкоуровневый доступ к `/dev/input/event*`
- Захват одного устройства не влияет на остальные
- Можем создавать виртуальные устройства для эмуляции
- Полный контроль над потоком событий

### Критически важная деталь: синхронизация
- **НИКОГДА не вызывать `ui.syn()` после каждого события**
- `syn()` вызывается только после полных пакетов (press+release или repeat)
- Это предотвращает артефакты в виде пустых строк в редакторах

## Финальный статус

✅ POC работает корректно:
- RCtrl+Backspace подавляется
- Остальные клавиши проходят в систему
- Автоповтор работает
- Нет пустых строк в редакторе
- Нет артефактов

📦 Файлы:
- `test_key_suppression_evdev.py` - рабочий POC
- `test_key_suppression.py` - неудачная попытка с pynput

🎯 Следующие шаги:
- Интегрировать подход в tap_launcher
- Поддержать конфигурируемые комбинации
- Обработать edge cases (многоклавишные комбинации)

