# Предложение по поддержке Wayland

**Дата:** 24 октября 2025  
**Статус:** Техническое предложение (требует обсуждения)

---

## Резюме

Предлагается добавить поддержку Wayland в `tap_launcher` и `tap_detector` через **backend-абстракцию**, позволяющую переключаться между:
- **PynputBackend** - для X11 (текущая реализация)
- **EvdevBackend** - для Wayland (новая реализация)

**Ключевое преимущество:** Минимальные изменения в существующем коде, вся логика tap detection остаётся нетронутой.

---

## Проблема

### Текущая ситуация
- `pynput` работает **только с X11**
- KUbuntu 25.10 использует Wayland по умолчанию
- tap_launcher **не работает** в Wayland-сессиях

### Почему важно решить
- Переход на Wayland неизбежен
- Необходима нативная поддержка современных дистрибутивов

---

## Предлагаемое решение

### Архитектура

```
┌─────────────────────────────────────────┐
│      TapMonitor (логика детекции)      │
│      (БЕЗ ИЗМЕНЕНИЙ!)                  │
└─────────────────┬───────────────────────┘
                  │
                  │ callbacks: on_press, on_release
                  │
┌─────────────────▼───────────────────────┐
│    KeyboardBackend (абстракция)        │
└─────────────────┬───────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
┌───────▼──────┐    ┌───────▼──────┐
│ PynputBackend│    │ EvdevBackend │
│    (X11)     │    │  (Wayland)   │
└──────────────┘    └──────────────┘
```

### Ключевая идея

Backend'ы преобразуют события из разных источников в **единый формат** (pynput Key objects), поэтому вся логика tap detection **не меняется**.

---

## Изменения в коде

### Что МЕНЯЕТСЯ (минимально)

#### 1. Новая структура файлов
```
src/tap_detector/
├── backends/              # ← НОВЫЙ каталог
│   ├── __init__.py
│   ├── base.py           # ← базовая абстракция
│   ├── pynput_backend.py # ← обёртка над pynput (X11)
│   ├── evdev_backend.py  # ← новый backend (Wayland)
│   ├── key_mapping.py    # ← маппинг evdev→pynput
│   └── detector.py       # ← автоопределение backend'а
```

#### 2. Изменения в TapMonitor (4 строки)

**До:**
```python
def start(self):
    from pynput import keyboard
    self.listener = keyboard.Listener(
        on_press=self._on_press,
        on_release=self._on_release,
        suppress=False
    )
    self.listener.start()
    self.listener.join()
```

**После:**
```python
def __init__(self, ..., backend=None):
    # ...
    self.backend = backend or create_backend()  # ← автоопределение

def start(self):
    self.backend.start(
        on_press=self._on_press,
        on_release=self._on_release
    )

def stop(self):
    self.backend.stop()
```

#### 3. Зависимости (pyproject.toml)
```toml
[project.optional-dependencies]
wayland = ["evdev>=1.7.0"]
```

### Что НЕ МЕНЯЕТСЯ

✅ `TapMonitor._on_press()` - без изменений  
✅ `TapMonitor._on_release()` - без изменений  
✅ Вся логика tap detection - без изменений  
✅ `key_normalizer.py` - без изменений  
✅ `LauncherMonitor` - без изменений  
✅ `HotkeyMatcher` - без изменений  
✅ Конфигурационные файлы - без изменений  

**Callbacks получают те же pynput Key objects независимо от backend'а!**

---

## Документация

### 1. Технический дизайн
📄 **`docs/wayland-backend-design.md`**
- Детальный дизайн архитектуры
- Полная спецификация backend'ов
- План реализации (8-12 часов)
- Риски и митигация

### 2. Примеры реализации
📁 **`docs/backend-examples/`**
- `01-base.py` - базовая абстракция
- `02-pynput-backend.py` - X11 реализация (можно запустить)
- `03-evdev-backend-simplified.py` - Wayland реализация (можно запустить)
- `04-integration-example.py` - интеграция с TapMonitor
- `README.md` - описание примеров

### 3. Proof-of-Concept тест
🧪 **`docs/poc-evdev-test.py`**
- Тест работоспособности evdev в системе
- Проверка прав доступа
- Поиск клавиатурных устройств
- Интерактивное тестирование событий

---

## Как проверить концепцию

### Шаг 1: Установить evdev

```bash
# Через uv (рекомендуется для проекта)
uv add --optional wayland evdev

# Или через pip
pip install evdev
```

### Шаг 2: Проверить права доступа

```bash
# Добавить пользователя в группу input
sudo usermod -a -G input $USER

# Перелогиниться для применения изменений
# (или перезапустить сессию)
```

### Шаг 3: Запустить PoC тест

```bash
cd /home/shtirliz/workspace/myself/tap_launcher

# Запустить тест evdev
python3 docs/poc-evdev-test.py
```

**Тест проверит:**
- ✓ Доступность библиотеки evdev
- ✓ Права доступа к `/dev/input/`
- ✓ Обнаружение клавиатурных устройств
- ✓ Чтение событий клавиатуры
- ✓ Интерактивное тестирование тапов

### Шаг 4: Попробовать примеры backend'ов

```bash
# Установить зависимости (pynput и evdev)
pip install pynput evdev

# Запустить примеры
python3 docs/backend-examples/02-pynput-backend.py
python3 docs/backend-examples/03-evdev-backend-simplified.py
python3 docs/backend-examples/04-integration-example.py
```

---

## Преимущества решения

### ✅ Минимальные изменения
- Только 4 строки в `TapMonitor`
- Вся логика tap detection не меняется
- Обратная совместимость гарантирована

### ✅ Чистая архитектура
- Разделение ответственности (backend ≠ логика)
- Легко тестировать (mock backends)
- Легко расширять (добавить Windows/macOS backend'ы)

### ✅ Прозрачность для пользователя
- Автоопределение среды (X11/Wayland)
- Не нужно ничего настраивать
- Работает "из коробки"

### ✅ Гибкость
- Можно явно выбрать backend через код/конфиг
- Можно указать конкретное устройство для evdev
- Можно добавить fallback-стратегии

---

## Требования для Wayland

### Права доступа

evdev требует доступа к `/dev/input/event*`:

```bash
# Вариант 1: Добавить в группу input (рекомендуется)
sudo usermod -a -G input $USER

# Вариант 2: udev rule (более гибко)
echo 'KERNEL=="event*", SUBSYSTEM=="input", MODE="0660", GROUP="input"' | \
  sudo tee /etc/udev/rules.d/99-input.rules
```

### Документация

При ошибке доступа пользователь получит понятное сообщение:

```
❌ Permission denied accessing /dev/input/
   
   Add user to "input" group:
   sudo usermod -a -G input $USER
   
   Then log out and back in.
```

---

## План реализации

### Этап 1: Backend-абстракция (2-3 ч)
- [x] Создать `backends/base.py`
- [x] Создать `backends/pynput_backend.py`
- [x] Создать `backends/key_mapping.py`
- [x] Создать `backends/detector.py`

### Этап 2: Evdev backend (3-4 ч)
- [ ] Реализовать `backends/evdev_backend.py`
- [ ] Добавить поиск устройств
- [ ] Протестировать маппинг всех клавиш

### Этап 3: Интеграция (1-2 ч)
- [ ] Модифицировать `TapMonitor`
- [ ] Обновить зависимости
- [ ] Обновить документацию

### Этап 4: Тестирование (2-3 ч)
- [ ] Тесты на X11
- [ ] Тесты на Wayland
- [ ] Edge cases и обработка ошибок

**Общее время:** 8-12 часов работы

---

## Альтернативы (рассмотрены и отклонены)

### ❌ Полная замена pynput на evdev
- Потеря кроссплатформенности
- Большие изменения в коде
- Сложнее в использовании

### ❌ Использование libinput
- Python bindings незрелые
- Те же требования к правам
- Более сложный API

### ❌ KWin scripts / compositor-specific
- Работает только в KDE
- Не переносимо
- Ограниченный API

---

## Вопросы для обсуждения

1. **Архитектура:**
   - Устраивает ли backend-абстракция?
   - Есть ли замечания по API?

2. **Маппинг ключей:**
   - Достаточно ли покрытие в `key_mapping.py`?
   - Нужны ли дополнительные клавиши?

3. **Права доступа:**
   - Приемлемо ли требование группы `input`?
   - Нужны ли альтернативные способы?

4. **Конфигурация:**
   - Нужна ли опция явного выбора backend'а?
   - Нужна ли опция выбора конкретного устройства?

5. **Тестирование:**
   - Какие edge cases важно покрыть?
   - Нужны ли автоматические тесты?

---

## Следующий шаг

### Для обсуждения (сейчас)
1. ✅ Изучить технический дизайн (`wayland-backend-design.md`)
2. ✅ Запустить PoC тест (`poc-evdev-test.py`)
3. ✅ Попробовать примеры (`backend-examples/`)
4. 🔄 Обсудить архитектурное решение
5. 🔄 Принять решение о реализации

### Для реализации (потом)
1. ⏸️ Реализовать backend-абстракцию
2. ⏸️ Интегрировать в TapMonitor
3. ⏸️ Протестировать на X11 и Wayland
4. ⏸️ Обновить документацию

---

## Решение в ConPort

Архитектурное решение зарегистрировано в ConPort:
- **ID:** 8
- **Теги:** wayland, architecture, backend-abstraction, evdev, x11
- **Статус:** Предложение (требует утверждения)

---

## Контакты и ресурсы

- **Технический дизайн:** `docs/wayland-backend-design.md`
- **Примеры кода:** `docs/backend-examples/`
- **PoC тест:** `docs/poc-evdev-test.py`
- **ConPort решение:** ID 8

**Готов обсудить детали и ответить на вопросы!** 🚀

