# Предложение по поддержке Wayland (v2 - исправленное)

**Дата:** 24 октября 2025  
**Версия:** 2.0 (исправленная архитектура)  
**Статус:** Техническое предложение (требует обсуждения)

---

## Резюме

Предлагается добавить поддержку Wayland в **оба приложения** (`tap_launcher` и `tap_detector`) через:
1. **Общий модуль** `src/common/` с backend-абстракцией
2. **PynputBackend** - для X11 (текущая реализация)
3. **EvdevBackend** - для Wayland (новая реализация)

**Ключевое преимущество:** Backend-код пишется **один раз** и используется **обоими** приложениями.

---

## Ключевое изменение от v1

### ❌ Неправильно (v1):
Backend'ы в `src/tap_detector/backends/` - только для одного приложения.

### ✅ Правильно (v2):
Backend'ы в `src/common/` - **общий код для обоих приложений**.

---

## Архитектура

### Структура проекта

```
src/
├── common/                      # ← НОВЫЙ: общий код
│   ├── __init__.py
│   ├── backends/                # Backend'ы клавиатуры
│   │   ├── __init__.py
│   │   ├── base.py             # Абстракция
│   │   ├── pynput_backend.py   # X11 реализация
│   │   ├── evdev_backend.py    # Wayland реализация
│   │   ├── key_mapping.py      # evdev→pynput маппинг
│   │   └── detector.py         # Автоопределение
│   └── key_normalizer.py       # ← ПЕРЕМЕСТИТЬ из tap_detector
│
├── tap_detector/                # Приложение 1
│   ├── __init__.py
│   ├── main.py
│   ├── tap_monitor.py          # использует common.backends
│   ├── formatter.py
│   └── constants.py
│
└── tap_launcher/                # Приложение 2
    ├── __init__.py
    ├── main.py
    ├── launcher_monitor.py     # использует common.backends
    ├── hotkey_matcher.py
    ├── command_executor.py
    ├── config_loader.py
    ├── daemon_manager.py
    └── models.py
```

### Диаграмма зависимостей

```
┌─────────────────────────────────────────┐
│         tap_detector (app 1)           │
│         TapMonitor                      │
└──────────────┬──────────────────────────┘
               │
               │ использует
               │
┌──────────────▼──────────────────────────┐
│      src/common/ (ОБЩИЙ КОД)           │
│                                         │
│   KeyboardBackend (абстракция)         │
│      ├─ PynputBackend (X11)            │
│      └─ EvdevBackend (Wayland)         │
│                                         │
│   key_normalizer (утилиты)             │
└──────────────┬──────────────────────────┘
               │
               │ использует
               │
┌──────────────▼──────────────────────────┐
│         tap_launcher (app 2)            │
│         LauncherMonitor                 │
└─────────────────────────────────────────┘
```

---

## Изменения в коде

### Что добавляется

#### 1. Новый модуль `src/common/`

**Backend'ы** (`common/backends/`):
- `base.py` - абстракция `KeyboardBackend`
- `pynput_backend.py` - обёртка над pynput для X11
- `evdev_backend.py` - реализация через evdev для Wayland
- `key_mapping.py` - маппинг evdev key codes → pynput Keys
- `detector.py` - автоопределение backend'а по среде

**Утилиты** (`common/`):
- `key_normalizer.py` - переместить из `tap_detector/`

#### 2. Изменения в `tap_detector/tap_monitor.py`

```python
# Было:
from .key_normalizer import normalize_key
from pynput import keyboard
# ...
self.listener = keyboard.Listener(...)

# Стало:
from common.key_normalizer import normalize_key  # ← изменить импорт
from common.backends.detector import create_backend
from common.backends.base import KeyboardBackend

def __init__(self, ..., backend: KeyboardBackend | None = None):
    # ...
    self.backend = backend or create_backend()  # ← добавить

def start(self):
    self.backend.start(...)  # ← использовать backend
```

#### 3. Изменения в `tap_launcher/launcher_monitor.py`

```python
# Было:
from tap_detector.key_normalizer import format_keys_display

# Стало:
from common.key_normalizer import format_keys_display  # ← изменить импорт

# TapMonitor уже использует backend - никаких других изменений!
```

#### 4. Изменения в `pyproject.toml`

```toml
[project.optional-dependencies]
wayland = ["evdev>=1.7.0"]

[tool.hatch.build.targets.wheel]
packages = [
    "src/common",          # ← ДОБАВИТЬ
    "src/tap_detector",
    "src/tap_launcher"
]
```

### Что НЕ меняется

✅ Вся логика tap detection в `TapMonitor`  
✅ Вся логика launcher в `LauncherMonitor`  
✅ Все callbacks (`_on_press`, `_on_release`)  
✅ `HotkeyMatcher`, `CommandExecutor`  
✅ Конфигурационные файлы  

---

## Преимущества архитектуры

### ✅ DRY (Don't Repeat Yourself)
- Backend-код написан **один раз**
- Используется **обоими** приложениями
- Нет дублирования логики

### ✅ Модульность
- `common` - независимый модуль
- Может быть переиспользован
- Легко тестировать отдельно

### ✅ Минимальные изменения
- TapMonitor: +3 строки
- LauncherMonitor: изменить 1 импорт
- Вся логика нетронута

### ✅ Обратная совместимость
- Backend создаётся автоматически
- Оба приложения работают на X11 без изменений
- Прозрачное добавление Wayland

---

## Документация

### 1. Технический дизайн (v2)
📄 **`docs/wayland-backend-design-v2.md`**
- Полная спецификация с учётом общего модуля
- Детальная структура `src/common/`
- Интеграция в оба приложения
- План реализации (6-8 часов)

### 2. Примеры реализации
📁 **`docs/backend-examples/`**
- Примеры backend'ов (исполняемые)
- Демонстрация интеграции
- ⚠️ Примеры из v1 - обновить с учётом `common/`

### 3. Proof-of-Concept тест
🧪 **`docs/poc-evdev-test.py`**
- Тест работоспособности evdev
- Проверка прав доступа
- Интерактивное тестирование

---

## Как проверить концепцию

### Шаг 1: Установить evdev

```bash
# Через uv (рекомендуется)
uv add --optional wayland evdev

# Или через pip
pip install evdev
```

### Шаг 2: Проверить права доступа

```bash
# Добавить в группу input
sudo usermod -a -G input $USER

# Перелогиниться для применения
```

### Шаг 3: Запустить PoC тест

```bash
cd /home/shtirliz/workspace/myself/tap_launcher
python3 docs/poc-evdev-test.py
```

---

## План реализации

### Этап 1: Создание общего модуля (2-3 ч)
1. Создать структуру `src/common/backends/`
2. Реализовать все backend'ы
3. Переместить `key_normalizer.py` в `common/`

### Этап 2: Интеграция в tap_detector (1 ч)
1. Модифицировать `tap_monitor.py`
2. Обновить импорты
3. Удалить старый `key_normalizer.py`

### Этап 3: Интеграция в tap_launcher (30 мин)
1. Обновить импорты в `launcher_monitor.py`

### Этап 4: Конфигурация (30 мин)
1. Обновить `pyproject.toml`
2. Обновить документацию

### Этап 5: Тестирование (2-3 ч)
1. Тесты обоих приложений на X11
2. Тесты обоих приложений на Wayland
3. Edge cases

**Общее время:** 6-8 часов

---

## Сравнение с v1

| Аспект | v1 (неправильно) | v2 (правильно) |
|--------|------------------|----------------|
| Расположение backend'ов | `tap_detector/backends/` | `src/common/backends/` |
| Переиспользование | Только tap_detector | Оба приложения |
| key_normalizer | Остаётся в tap_detector | Переносится в common |
| DRY принцип | ❌ Нарушен | ✅ Соблюдён |
| Модульность | ❌ Слабая | ✅ Сильная |

---

## Вопросы для обсуждения

1. **Структура:**
   - Устраивает ли структура `src/common/`?
   - Что ещё вынести в `common`?

2. **Импорты:**
   - Как импортировать: `from common.backends import ...`?
   - Или: `from common import backends`?

3. **Тестирование:**
   - Нужны ли unit-тесты для `common`?
   - Как организовать тесты?

4. **Документация:**
   - Достаточно ли описания?
   - Что ещё уточнить?

---

## Следующий шаг

### Для обсуждения (сейчас)
1. ✅ Изучить v2 технического дизайна
2. ✅ Проверить структуру `src/common/`
3. 🔄 Обсудить архитектурное решение
4. 🔄 Принять решение о реализации

### Для реализации (потом)
1. ⏸️ Создать `src/common/` модуль
2. ⏸️ Интегрировать в оба приложения
3. ⏸️ Протестировать на X11 и Wayland
4. ⏸️ Обновить документацию

---

## Решение в ConPort

Архитектурное решение обновлено в ConPort:
- **ID:** 8
- **Версия:** 2.0 (исправлена архитектура)
- **Теги:** wayland, architecture, backend-abstraction, evdev, x11, common-module
- **Статус:** Предложение (требует утверждения)

---

## Контакты и ресурсы

- **Технический дизайн v2:** `docs/wayland-backend-design-v2.md`
- **Примеры кода:** `docs/backend-examples/` (требуют обновления)
- **PoC тест:** `docs/poc-evdev-test.py`
- **ConPort решение:** ID 8 (обновлено)

**Готов обсудить детали v2 архитектуры!** 🚀

